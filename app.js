const GameBoard = (function() {
    const board = [];
    // How to create interactivity?
    // How will Player use the gameboard?
    return {
        gameBoard : function() {
            board;
        }
    }
})();

const Player = function(name, marker) {
    // what can a player do?
    // what can a player influence?
}

const controlFlow = (function() {
    // How to play the game? 
    // What are the steps needed to progress the game further?
})();
